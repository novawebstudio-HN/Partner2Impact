{
  "name": "partner2impact-tools",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling. The site itself has no dependencies.",
  "scripts": {
    "check": "node design-check.mjs",
    "shots": "node design-check.mjs --shots"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
